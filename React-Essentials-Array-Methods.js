// Map, Filter, Reduce -> Array Methods

const bachhaLog = ['gouranga, mahesh, alien, anshul, bachha, pencil, chota bachha, kundru'];

const removeText = bachhaLog.map(function (name) {
    return name
})

console.log(removeText)